Wavelength,Average,Red,Green,Blue
<%

data = ActiveSupport::JSON.encode(@spectrum.data)['lines']

data.each do |datum|

w = datum['wavelength'].to_s[0..6]
a = datum['average'].to_s[0..3]
r = datum['r']
g = datum['g']
b = datum['b']

%><%= w %>,<%= a %>,<%= r %>,<%= g %>,<%= b %>
<% end %>
