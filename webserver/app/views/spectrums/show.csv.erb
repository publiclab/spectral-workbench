<%

data = ActiveSupport::JSON.decode(@spectrum.data)["lines"]

data.each do |datum|

w = datum['wavelength'].to_s[0..6]
a = datum['average'].to_s[0..3]
w = 0 if w = ""

%><%= w %>,<%= a %>
<% end %>
